[project]
name = "lc0-server"
version = "0.1.0"
description = "FastAPI wrapper around an lc0 UCI subprocess for latent-state extraction"
requires-python = ">=3.10"
dependencies = [
    "chess>=1.11.2",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.0",
    "httpx>=0.27.0",
]
